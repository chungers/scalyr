
# Install Docker EE
docker-ee: docker-ee.sh

# Install Scalyr on local host
install: install-scalyr.sh

# Install Docker EE and Scalyr
all : install-all.sh

# Parameterized agent.json file
agent.json : agent.json
